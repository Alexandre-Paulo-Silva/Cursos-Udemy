const nome = 'Alexandre';
const concatenacao = 'alo' + nome + '!';
const template = `ola ${nome}`

console.log(concatenacao);

console.log(`1+1 = ${1+1}`);

const up = texto => texto.toUpperCase();
console.log(`Ei... ${up('cuidado')}! `);